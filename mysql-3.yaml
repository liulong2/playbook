---
- name: mysql
  hosts: mysql3
  become: true
  tasks:
    - name: MySQL-python
      #      yum:
      #        name: MySQL-python
      #      #        state: installed
      #        state: installed
      pip:
        name: MySQL-python
    - name: dump a database
      mysql_db:
        login_host: 192.168.0.206
        login_user: root
        login_password: "root"
        login_port: 3306
        name: all
        target: /tmp/mezz$(date +%Y-%m-%d).sql
        state: "dump"
    - name: fetch
      fetch:
        dest: /tmp/
        src: /tmp/mezz.sql

